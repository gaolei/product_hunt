var as_product_name;
if (as_product_name == undefined) {
  as_product_name = new Awesomplete(document.getElementById("product_name"), {minChars: 0, list: <%= raw @product_names.to_s -%>});
} else {
  as_product_name.list = <%= raw @product_names.to_s -%>;
}
